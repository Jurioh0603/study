# 문자열 활용
food = 'python\'s favorite food is says.' # 따옴표 포함하고 싶을 때 역슬래시 활용
food = "python's favorite food is says." # 따옴표 포함하고 싶을 때 쌍 따옴표 활용

food =  '''
python's
favorite food
is says.
'''
print(food) # python's  # 여러줄 출력시 따옴표 3개
            # favorite food
            # is says.


a = 'python'
print(a * 2) # pythonpython

print(len(a)) # 6


# 제곱 구하기
print(2 ** 3) # 8 # ** => 제곱

# 몫 구하기 : /의 결과타입은 float, //의 결과 타입은 int
print(14 / 2) # 7.0
print(14 // 2) # 7

# 나머지 연산자
print(14 % 3) # 2
